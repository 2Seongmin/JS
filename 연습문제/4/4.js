
const startBtn = document.querySelector("#startBtn");

// user 가위바위보 버튼
const userInput = document.querySelectorAll(".btn");

startBtn.addEventListener("click", () => {

    const input = Number(prompt("진행할 판 수를 입력하세요"));

    // 모든 변수 초기화

    
    for(let i=0; i <= input; i++){

    };
    
});